{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "My Task",
            "command": "${command:python.interpreterPath}",
            "type": "shell",
            "args": ["${file}"],
            "presentation": {
                "reveal": "always", "focus": true
            },
            "group": {"isDefault": true, "kind": "build"}
        }
    ]
}